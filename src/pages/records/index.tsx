import React from "react";
import { Layout } from "../../components/Layout/Layout";

const Records = () => {
  return (
    <Layout title="Рекорды и Карты">
      <span style={{ color: "white" }}>Тут скоро будут Рекорды и Карты, а пока тут пусто {")"}</span>
    </Layout>
  );
};

export default Records;
